%h1= @menu.title
%h2 There should probably be a picture(or a bunch)
%h3 And oh yeah, ingredients too.

= link_to "Good Stuff", user_menu_responses_path(accepted: true, menu_id: @menu.id, user_id: @user.id), method: :post
= link_to "Nope, Definitely Not", user_menu_responses_path(accepted: false, menu_id: @menu.id, user_id: @user.id), method: :post


